# ⌨️ Keyboard Event Logger (Educational Project)

This Python project demonstrates how to capture and log keyboard input events using the **pynput** library.  
It is intended **only for educational purposes** to understand how keyboard event handling works in Python.

## 🚀 Features
- Captures keystrokes in real-time  
- Logs keys pressed into a text file (`log.txt`)  
- Displays pressed keys in the terminal for debugging  

## ⚠️ Disclaimer
This project is for **learning and demonstration only**.  
Do not use it to collect sensitive information or monitor others without consent. Misuse of this type of software may violate privacy laws and ethical guidelines.

## 🛠️ Installation
Clone the repository and install dependencies:

```bash
git clone https://github.com/your-username/keyboard-logger.git
cd keyboard-logger
pip install pynput
